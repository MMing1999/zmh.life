---
layout: layouts/base.njk
title: "观察 · 知行合一"
description: "世界观察 - 摄影、短文、生活记录"
section: "zhi"
pageName: "observation"
---

<div class="observation-intro">
  <h1>观察 · Observation</h1>
  <p>这里是我对于这个世界的观察。</p>
</div>

<div class="observation">
  <div class="part1">
    <div class="title-list">
      {% for post in collections.observation %}
        <div class="title-item">
          <a href="{{ post.url }}">
            <span class="post-title">{{ post.data.title }}</span>
          </a>
        </div>
      {% endfor %}
    </div>
  </div>
  
  <div class="part2">
    <div class="image-grid">
      {% for post in collections.observation %}
        <div class="image-item">
          <a href="{{ post.url }}">
            {% if post.data.coverImage %}
              {% image post.data.coverImage, "观察图片", "(min-width: 768px) 50vw, 100vw" %}
            {% else %}
              <div class="placeholder-image">
                <span>{{ post.data.title }}</span>
              </div>
            {% endif %}
          </a>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

<style>
.observation-intro {
  height: 30vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 0 0 1rem 0;
  border-bottom: 1px solid #eee;
  margin-bottom: 2rem;
}

.observation-intro h1 {
  font-size: 1.8rem;
  font-weight: 400;
  color: #333;
  margin: 0 0 0.5rem 0;
}

.observation-intro p {
  font-size: 1rem;
  color: #666;
  margin: 0;
  line-height: 1.4;
}

.observation {
  display: flex;
  gap: 2rem;
  min-height: 80vh;
  padding: 2rem 0;
}

.part1 {
  flex: 2;
  padding-right: 2rem;
}

.title-list {
  display: flex;
  flex-direction: column;
  gap: 0;
}

.title-item {
  padding: 0;
  margin-bottom: 0.5rem;
}

.title-item:last-child {
  margin-bottom: 0;
}

.title-item a {
  text-decoration: none;
  color: #333;
  font-size: 0.9rem;
  line-height: 1.4;
  display: block;
  transition: color 0.2s ease;
}

.title-item a:hover {
  color: #999;
}

.post-title {
  width: 100%;
}

.part2 {
  flex: 8;
  min-width: 0;
}

.image-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 1rem;
}

.image-item {
  aspect-ratio: 3/4;
  overflow: hidden;
  border-radius: 4px;
  background: #f5f5f5;
  transition: all 0.3s ease;
}

.image-item a {
  display: block;
  width: 100%;
  height: 100%;
  text-decoration: none;
}

.image-item img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.3s ease;
}

.image-item:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(0,0,0,0.15);
}

.image-item:hover img {
  transform: scale(1.05);
}

.placeholder-image {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #f0f0f0;
  color: #999;
  font-size: 0.8rem;
  text-align: center;
  padding: 1rem;
}

/* 响应式设计 */
@media (max-width: 1200px) {
  .image-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}

@media (max-width: 900px) {
  .image-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* iPad 端保持 2:8 比例 */
@media (max-width: 1024px) and (min-width: 769px) {
  .part1 {
    flex: 2;
  }
  
  .part2 {
    flex: 8;
  }
  
  .image-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}

/* 手机端改为上下布局 */
@media (max-width: 768px) {
  .observation {
    flex-direction: column;
    gap: 1rem;
  }
  
  .part1 {
    flex: none;
    padding-right: 0;
    padding-bottom: 1rem;
  }
  
  .part2 {
    flex: none;
  }
  
  .image-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 0.5rem;
  }
}

@media (max-width: 480px) {
  .image-grid {
    grid-template-columns: 1fr;
  }
}
</style>
